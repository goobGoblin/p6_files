controlTest : () -> int {
  counter : int;
  limit : int;
  sum : int;
  
  counter = 1;
  limit = 5;
  sum = 0;
  
  while (counter <= limit) {
    sum = sum + counter;
    counter = counter + 1;
  }
  
  if (sum > 10) {
    sum = sum * 2;
  } else {
    sum = sum / 2;
  }
  
  return sum;
}
